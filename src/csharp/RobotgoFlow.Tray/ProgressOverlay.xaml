<Window x:Class="RobotgoFlow.Tray.ProgressOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" Width="280" Height="80"
        WindowStyle="None" ShowInTaskbar="False"
        Topmost="True" AllowsTransparency="True"
        Background="#CC1A1A2E" ResizeMode="NoResize">
    <Grid Margin="8,4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Ellipse x:Name="StatusDot" Width="8" Height="8" Margin="0,0,6,0"
                     VerticalAlignment="Center">
                <Ellipse.Style>
                    <Style TargetType="Ellipse">
                        <Setter Property="Fill" Value="{StaticResource PrimaryGreen}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Status}" Value="error">
                                <Setter Property="Fill" Value="{StaticResource ErrorRed}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Status}" Value="success">
                                <Setter Property="Fill" Value="{StaticResource PrimaryGreen}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Ellipse.Style>
            </Ellipse>
            <TextBlock x:Name="StepNameText" Text="{Binding StepName}"
                       Foreground="#E0E0E0" FontSize="12" VerticalAlignment="Center"
                       MaxWidth="200" TextTrimming="CharacterEllipsis"/>
        </StackPanel>

        <TextBlock x:Name="ActionText" Grid.Row="1" Text="{Binding CurrentAction}"
                   Foreground="{StaticResource TextDim}" FontSize="10"
                   MaxWidth="260" TextTrimming="CharacterEllipsis"
                   Margin="0,2,0,0"/>
        <TextBlock x:Name="EstimatedText" Grid.Row="1" Text="{Binding EstimatedText}"
                   Foreground="{StaticResource TextSecondary}" FontSize="10"
                   HorizontalAlignment="Right" Margin="0,0,0,0"/>

        <Grid Grid.Row="2" Margin="0,4,0,0">
            <ProgressBar x:Name="ProgressBar" Height="4"
                         Minimum="0" Maximum="1" Value="{Binding Progress}"
                         Background="#333355">
                <ProgressBar.Style>
                    <Style TargetType="ProgressBar">
                        <Setter Property="Foreground" Value="{StaticResource PrimaryGreen}"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Status}" Value="error">
                                <Setter Property="Foreground" Value="{StaticResource ErrorRed}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Status}" Value="success">
                                <Setter Property="Foreground" Value="{StaticResource PrimaryGreen}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ProgressBar.Style>
            </ProgressBar>
            <TextBlock x:Name="ProgressTextBlock" Text="{Binding ProgressText}"
                       Foreground="{StaticResource TextDim}" FontSize="10"
                       HorizontalAlignment="Right" Margin="0,0,0,-2"/>
        </Grid>
    </Grid>
</Window>
